% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fit_lda_warp}
\alias{fit_lda_warp}
\title{Fit an LDA model with the warpLDA sampler}
\usage{
fit_lda_warp(
  dtm_in,
  Cd_start,
  alpha_in,
  eta_in,
  iterations,
  burnin,
  calc_likelihood,
  Beta_in,
  freeze_topics = FALSE,
  likelihood_every = 10L,
  mh_steps = 1L,
  verbose = TRUE
)
}
\arguments{
\item{dtm_in}{arma::sp_mat document term matrix, documents by words}

\item{Cd_start}{IntegerMatrix, documents by topics. Initial document-topic
counts, \code{theta_initial * rowSums(dtm)} from the R side}

\item{alpha_in}{Vector of prior parameters for topics over documents}

\item{eta_in}{NumericMatrix, topics by words. Prior for words over topics}

\item{iterations}{int number of sampling iterations. Zero initializes and
returns without sampling, which is how the initialization is inspected}

\item{burnin}{int number of burn in iterations, -1 to disable averaging}

\item{calc_likelihood}{bool, calculate log likelihood?}

\item{Beta_in}{NumericMatrix, topics by words. The fitted beta, used only
when \code{freeze_topics = TRUE}}

\item{freeze_topics}{bool, hold topics fixed for prediction?}

\item{likelihood_every}{int, evaluate the likelihood every n-th iteration}

\item{mh_steps}{int, Metropolis-Hastings proposals per token per pass}

\item{verbose}{bool, show a progress bar?}
}
\value{
Returns a list with the same names as \code{fit_lda_c}.
}
\description{
Metropolis-Hastings replacement for \code{fit_lda_c}. Phase 2:
  single-threaded, scalar \code{eta} only.
}
\keyword{internal}
